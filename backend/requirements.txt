fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# --- Pesado: necesario SOLO si el modelo ganador es BERT (meta.json type=="bert").
# El ganador se decide dinámicamente en el notebook, así que se incluyen por
# seguridad. Si el artefacto activo es type=="sklearn", puedes borrar estas dos
# líneas para un deploy ~270 MB más ligero (torch nunca se importa en ese caso).
torch>=2.2
transformers>=4.46
